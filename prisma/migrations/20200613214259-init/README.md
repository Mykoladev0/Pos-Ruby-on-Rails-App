# Migration `20200613214259-init`

This migration has been generated by Nate Stewart at 6/13/2020, 9:42:59 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."Store" (
"accessToken" TEXT NOT NULL  ,"createdAt" TEXT   ,"hash" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"updatedAt" TEXT   ,"url" TEXT   )

CREATE TABLE "quaint"."User" (
"context" TEXT NOT NULL  ,"createdAt" TEXT   ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"storeId" INTEGER NOT NULL  ,"updatedAt" TEXT   ,"userData" TEXT NOT NULL  ,FOREIGN KEY ("storeId") REFERENCES "Store"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE UNIQUE INDEX "quaint"."Store.hash" ON "Store"("hash")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200613214259-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,30 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "sqlite"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Store {
+  id            Int     @default(autoincrement()) @id
+  hash          String  @unique
+  url           String?
+  createdAt    String?
+  updatedAt    String?
+  accessToken  String
+}
+
+model User {
+  id          Int   @default(autoincrement()) @id
+  store       Store @relation(fields: [storeId], references: [id])
+  storeId    Int
+  context     String
+  userData   String
+  createdAt  String?
+  updatedAt  String?
+}
```


