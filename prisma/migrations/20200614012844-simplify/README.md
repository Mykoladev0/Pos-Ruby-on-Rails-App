# Migration `20200614012844-simplify`

This migration has been generated by Nate Stewart at 6/14/2020, 1:28:44 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_User" (
"context" TEXT NOT NULL  ,"createdAt" TEXT   ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"storeId" INTEGER NOT NULL  ,"updatedAt" TEXT   ,"userData" TEXT NOT NULL  )

INSERT INTO "quaint"."new_User" ("context", "createdAt", "id", "storeId", "updatedAt", "userData") SELECT "context", "createdAt", "id", "storeId", "updatedAt", "userData" FROM "quaint"."User"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."User";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_User" RENAME TO "User";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200613214259-init..20200614012844-simplify
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -13,18 +13,17 @@
 model Store {
   id            Int     @default(autoincrement()) @id
   hash          String  @unique
   url           String?
-  createdAt    String?
-  updatedAt    String?
-  accessToken  String
+  createdAt     String?
+  updatedAt     String?
+  accessToken   String
 }
 model User {
   id          Int   @default(autoincrement()) @id
-  store       Store @relation(fields: [storeId], references: [id])
-  storeId    Int
+  storeId     Int
   context     String
-  userData   String
-  createdAt  String?
-  updatedAt  String?
+  userData    String
+  createdAt   String?
+  updatedAt   String?
 }
```


